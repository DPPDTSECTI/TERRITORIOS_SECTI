[build]
  command = "npm run build"
  functions = ".netlify/functions"
  publish = "dist"

[functions]
  node_bundler = "esbuild"
  
[[redirects]]
  from = "/api/sharepoint"
  to = "/.netlify/functions/sharepoint"
  status = 200
  force = true
